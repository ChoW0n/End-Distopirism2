# CLAUDE.md

End-Distopirism 재개발 저장소. 이 파일은 Claude Code가 이 리포에서 작업할 때 따르는 규칙이다.

---

## 프로젝트

림버스 컴퍼니 / 라이브러리 오브 루이나 모티브의 **턴제 카드 전투 게임**.
2024년 유니티(2022.3.18f1, URP)로 만들어졌고, 카메라 버그로 전면 재설계를 시작했다가 인력 부족으로 2024-12-18에 중단됐다.
지금 하는 일은 이걸 **웹으로 포팅하면서 버그를 잡고 재개발**하는 것.

**제약: 추후 다시 유니티로 전환 가능해야 한다.** 이게 모든 구조 결정의 상위 조건이다.

- 원본 리포: `ChoW0n/End-Distopirism` (읽기 전용 참고용)
- 작업 리포: `ChoW0n/End-Distopirism2`

---

## 개발 방식

**스펙 주도 개발(SDD)** + 객체지향.

1. 스펙 문서를 먼저 확정한다
2. 스펙에 없는 동작은 구현하지 않는다
3. 구현이 스펙과 다르면 **코드가 틀린 것**이다
4. 스펙을 바꿔야 하면 문서를 먼저 고치고 커밋한다

전투 규칙의 단일 기준은 `docs/SPEC-001-전투시스템.md`. 전투 관련 작업 전에 반드시 읽는다.

---

## 아키텍처 규칙

```
도메인 (순수 로직)  ←  여기에 렌더러 의존성이 들어가면 유니티 재전환이 죽는다
   ↓ 이벤트만 내보냄
렌더링 어댑터 (웹: 2.5D)
   ↓
UI / 연출
```

**절대 금지**
- 도메인 계층에서 DOM, three.js, 브라우저 API, 렌더링 관련 타입을 import 하는 것
- 도메인이 렌더러를 직접 호출하는 것 (이벤트를 내보내고 어댑터가 구독한다)
- 게임 수치를 코드에 하드코딩하는 것 (데이터는 JSON)

**지켜야 할 것**
- 도메인은 입력을 받아 상태와 이벤트 목록을 반환하는 순수 함수/클래스로 짠다
- 턴 진행은 상태머신으로. 원본의 `BattleManager`(1,232줄)가 상태·연출·UI·입력을 다 들고 있었던 게 재설계가 실패한 이유다
- 카메라도 자체 상태머신 + `transform`에 쓰는 지점을 하나로 (원본 카메라 버그의 직접 원인)

### 2.5D는 유지한다
perspective 카메라로 바닥·벽·배경과 2D 캐릭터의 각도를 활용해 입체감을 내는 건 레퍼런스 두 작품의 표준 기법이고 이 게임의 핵심 인상이다. **직교 2D로 바꾸지 않는다.**
원본의 카메라 버그는 2.5D 탓이 아니라 카메라 상태 소유권이 없어서 생긴 것이다.

---

## 코드 주석 규칙

**한국어로 무조건 작성한다.** 비전공 협업자도 읽는다는 걸 전제로 쓴다.

```ts
//이름, 체력, 공격력 구현
constructor(name: string, hp: number, atk: number) { }

//코인을 굴려 성공 개수를 센다. 정신력이 낮을수록 성공률이 떨어진다
rollCoins(): number { }
```

- `//{내용}` 형식. 간결하게, 이 기능이 뭘 하는지만
- **`//서머리: {내용}//` 같은 AI식 작성법 금지**
- 기존 주석 어투를 참고해서 톤을 맞춘다
- 클래스·생성자·주요 메서드에는 빠짐없이 단다

---

## 스프라이트 / 이미지 규칙

- 기준일: **2026-09-14**. 이 시점에 보유한 스프라이트 톤에 맞춘다
- 톤: 흑백/브라운 기반, 강한 명암 대비, 거친 드로잉, 균열·녹·벗겨진 페인트
- 신규 스프라이트 생성 시 위 톤을 명시적으로 지시한다
- **예외: 퀄리티가 낮은 카드 스프라이트는 톤 기준점에서 제외** (그걸 따라가면 안 됨)

---

## 응답 규칙

- **존칭을 쓰지 않는다.** 불친절하지 않을 정도의 단답형
- 한국어
- 작업 보고는 결과만. 과정 나열 안 함

---

## 커밋 규칙 (원본 팀 규칙 승계)

```
[Feat] 새 기능
[Fix] 버그 수정
[Docs] 문서
[Style] 포맷팅, 로직 변경 없음
[Refactor] 리팩터링
[Test] 테스트 코드
[Chore] 빌드·패키지
```

- 메시지는 한국어
- 이슈 번호는 꼬리말에 `Resolves: #12` / `Fixes: #12` / `Ref: #12`

---

## 알려진 함정

원본에서 실제로 사람을 태운 것들. 포팅 중에 그대로 옮기지 않도록 주의.

| 함정 | 내용 |
|---|---|
| 코인 확률 | 원본은 `0.6 × 100`을 확률로 써서 **항상 100% 성공**이었다. 정신력 시스템이 통째로 죽어 있었음 |
| 레벨차 보너스 | 조건은 상대 방어력, 계산은 자기 방어력. 게다가 미충족 시 0으로 초기화 안 함 |
| 동점 판정 | `>` / `else` 2분기라 동점이 적 승리로 표시됨 |
| 카메라 | 커서추종 Lerp가 매 프레임 `transform`을 덮어쓰는데 복귀는 DOTween 트윈으로 함. 둘이 싸움 |
| `.meta` | 유니티 쪽 작업 시 `Assets/` 아래 `.meta`는 **반드시 커밋**. 날리면 모든 참조가 끊긴다 |
| 리포 용량 | 원본이 647MB. `Library/`와 발표·도록 자료가 통째로 들어가 있었다. `.gitignore` 확인할 것 |

---

## 작업 전 확인

1. `docs/SPEC-001-전투시스템.md` — 전투 관련이면 필수
2. `docs/00_현황파악_보고서.md` — 원본이 어떤 상태인지
3. 스펙의 TBD 항목이 미결이면 **구현하지 말고 먼저 묻는다**
